#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/sh" ]; then
  "$basedir//bin/sh"  "$basedir/../node-man/bin/node-man" "$@"
  ret=$?
else 
  /bin/sh  "$basedir/../node-man/bin/node-man" "$@"
  ret=$?
fi
exit $ret
